{
  "development": {
    "username": "${DB_DEV_USER}",
    "password": "${DB_DEV_PASSWORD}",
    "database": "${DB_DEV_NAME}",
    "host": "${DB_DEV_HOST}",
    "dialect": "${DB_DEV_DIALECT}"
  },
  "test": {
    "username": "${DB_TEST_USER}",
    "password": "${DB_TEST_PASSWORD}",
    "database": "${DB_TEST_NAME}",
    "host": "${DB_TEST_HOST}",
    "dialect": "${DB_TEST_DIALECT}"
  },
  "stage": {
    "username": "${DB_STAGE_USER}",
    "password": "${DB_STAGE_PASSWORD}",
    "database": "${DB_STAGE_NAME}",
    "host": "${DB_STAGE_HOST}",
    "dialect": "${DB_STAGE_DIALECT}"
  },
  "production": {
    "username": "${DB_PROD_USER}",
    "password": "${DB_PROD_PASSWORD}",
    "database": "${DB_PROD_NAME}",
    "host": "${DB_PROD_HOST}",
    "dialect": "${DB_PROD_DIALECT}"
  }
}